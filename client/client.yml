server:
  host: 49.51.243.227
  port: 30922
crypto:
  password: "buildingos" # 必须与客户端保持一致
tunnels:
  - type: http
    remoteDomain: aj.cros.buildingos.ai  # 唯一域名（必须唯一）
    localHost: 127.0.0.1
    localPort: 8787